# Student Records Manager

A simple C++ console application to manage student records. 
It allows users to input student names, grade levels, and GPA, 
then displays the information in a formatted way using string operations, enums, and custom namespaces.
